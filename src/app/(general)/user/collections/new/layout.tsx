type LayoutProps = { children: React.ReactNode };

export const metadata = {
  title: 'Kollektion erstellen',
  description: 'Kollektion erstellen'
};

const Layout = ({ children }: LayoutProps) => children;

export default Layout;
