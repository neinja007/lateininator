type LayoutProps = { children: React.ReactNode };

export const metadata = {
  title: 'Adjektive',
  description: 'Adjektive'
};

const Layout = ({ children }: LayoutProps) => children;

export default Layout;
