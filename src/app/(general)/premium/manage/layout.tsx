type LayoutProps = { children: React.ReactNode };

export const metadata = {
  title: 'Premium-Version verwalten',
  description: 'Premium-Version verwalten'
};

const Layout = ({ children }: LayoutProps) => children;

export default Layout;
