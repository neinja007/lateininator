type LayoutProps = { children: React.ReactNode };

export const metadata = {
  title: { absolute: 'Weiterleitung...' },
  description: 'Weiterleitung...'
};

const Layout = ({ children }: LayoutProps) => children;

export default Layout;
